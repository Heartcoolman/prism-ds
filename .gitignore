node_modules
dist
storybook-static
*.log
.DS_Store

# design-sync is local tooling state — not part of the public repo
.design-sync/
.ds-sync/
ds-bundle/

# docs site build output (VitePress)
docs/.vitepress/dist/
docs/.vitepress/cache/

# stray static output
site/
